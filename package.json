{
  "name": "khshagnor",
  "private": true,
  "version": "1.0.0",
  "description": "KHSHAGNOR Import, Export & Production — company website",
  "scripts": {
    "start": "serve . -l tcp://0.0.0.0:${PORT:-3000}",
    "dev": "serve . -l tcp://0.0.0.0:8080"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
